Files

settings.py
urls.py

2. Configuration


a. get_schema_view parameters
- info
- url
- patterns
- urlconf
- public
- validators
- generator_class
- authentication_classes
- permission_classes

b. SchemaView options
- SchemaView.with_ui(renderer, cache_timeout, cache_kwargs)
- SchemaView.without_ui(cache_timeout, cache_kwargs) 
- SchemaView.as_cached_view(cache_timeout, cache_kwargs, **initkwargs) 
- as_view

cache_timeout and cache_kwargs; if present, these are passed on to Django’s cached_page decorator in order to enable caching

3. Caching
4. Validation
can be activated by passing validators=['ssv'] to get_schema_view; if the generated schema is not valid, a SwaggerValidationError is raised by the handling codec
5. Online
If your schema is publicly accessible, swagger-ui will automatically validate it against the official swagger online validator and display the result in the bottom-right validation badge.

6. Local
run a local copy of swagger-validator and set the VALIDATOR_URL

$ docker run --name swagger-validator -d -p 8189:8080 --add-host test.local:10.0.75.1 swaggerapi/swagger-validator
84dabd52ba967c32ae6b660934fa6a429ca6bc9e594d56e822a858b57039c8a2
$ curl http://localhost:8189/debug?url=http://test.local:8002/swagger/?format=openapi
{}

7. Using swagger-cli
npm install -g swagger-cli

swagger-cli validate http://test.local:8002/swagger.yaml
http://test.local:8002/swagger.yaml is valid

